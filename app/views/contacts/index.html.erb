<h1>All Contacts</h1>
<table>

  <tr>
    <th>Name</th>
    <th>Phone</th>
  </tr>

  <%= @contacts.each do |c| %>
  <tr>
    <td><%= c.name %></td>
    <td><%= c.phone %></td>
  </tr>
  <% end %>
</table>
