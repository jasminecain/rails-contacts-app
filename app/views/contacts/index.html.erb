<h1>All Contacts</h1>

<p><%= link_to 'Add a contact', new_contact_path %></p>
<%# new(inner directory)
_contact(outer directory)
_path  %>

<table>

  <tr>
    <th>Name</th>
    <th>Phone</th>
  </tr>

  <%= @contacts.each do |c| %>
  <tr>
    <td><%= c.name %></td>
    <td><%= c.phone %></td>
  </tr>
  <% end %>
</table>
